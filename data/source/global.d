module global;
import menu.screens.options;

//Used to position text in screen
enum widthScreen = 1080;
enum heightScreen = 720;

int positionLabelX = widthScreen / 2 - 50,
    positionLabelY = heightScreen / 2;

//Used to create a space between each labels (variation)
int spacePosY = 20;

//Used to change FontSize of label in screen (default 20)
int fontSize = 20;